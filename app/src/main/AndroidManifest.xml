<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TakeMeWithYou"
        tools:targetApi="31">
        <activity
            android:name=".presentation.signup.SignUpActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.signin.SignInActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".presentation.detail.DetailPage"
            android:exported="true" />
        <activity
            android:name=".presentation.setting.SettingActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.mypage.MyPageActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.main.MainpageActivity"
            android:exported="false">
        </activity>
        <activity android:name=".Postpageadd"
            android:exported="false"/>
    </application>
</manifest>